package container

// Inventory maintains a real-time inventory of running containers and images.

// TODO: Implement container inventory
// - List running containers with metadata (ID, image, ports, volumes, labels)
// - Track image versions and layers
// - Report to control plane for fleet-wide container visibility
// - Detect stale/outdated images with known vulnerabilities
